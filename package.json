{
  "name": "lambda-glycobot",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "package": "pack-lambda --exclude-version --exclude-scope",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "compromise": "^11.9.0",
    "node-fetch": "^2.1.2",
    "request": "^2.85.0"
  },
  "bundledDependencies": [
    "request",
    "node-fetch",
    "compromise"
  ],
  "devDependencies": {
    "pack-lambda": "^1.0.0"
  }
}
